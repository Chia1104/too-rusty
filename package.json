{
    "name": "image-resize",
    "private": true,
    "version": "0.0.0",
    "main": "pkg/main.js",
    "scripts": {
      "dev": "cargo watch -i .gitignore -i 'pkg/*' -s 'wasm-pack build'",
      "build": "wasm-pack build",
      "clean": "cargo clean && rm -rf pkg"
    }
  }